spring:
  datasource:
    url: jdbc:h2:file:./data/tic-tac-toe-db
    username: sa
    password: P4ssw0rd!
    driverClassName: org.h2.Driver
  h2:
    console.enabled: true
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: true
    hibernate:
      ddl-auto: update

tic-tac-toe:
  board-size: 3